# Copy to .env and add your API key (used by the RAG backend)
OPENAI_API_KEY=your_openai_api_key_here

# RAG ingestion backend (docsearch + ingestion agents)
# Default: http://localhost:8001/copilotkit
# RAG_BACKEND_URL=http://localhost:8001/copilotkit
