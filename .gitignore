# Environment files - NEVER commit API keys or secrets
.env
.env.local
.env.*.local
.env.*
# Template file only - use placeholder values, never real secrets
!.env.example

# Python
__pycache__/

# Node / Next.js
node_modules/
.next/
out/

# Chat history (user-specific, optional to persist)
.chat_history.json
